# Reste à faire
- Sélection multiple de ports avec un carré de sélection
- Conteneurisation de l'application avec base de données (Docker Compose)
- Stockage des configurations dans la base de données
- Création de plusieurs configurations pour un ou plusieurs profils

## Options à ajouter :
- IP de l'interface des VLANs
- Mode dhcp pour l'interface des VLANs
- Les utilisateurs
    --> supprimer un utilisateur 
    --> créer un utilisateur 
    --> associer l'utilisateur à un groupe *
    --> spécifier le mdp (plaintext) *
    --> créer/modifier un groupe
        &> entrée d'une commande pour assigner/retirer des droits
    --> supprimer un groupe (que ceux créés)